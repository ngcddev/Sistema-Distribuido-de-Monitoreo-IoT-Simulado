name: CI

# Se dispara en cada Pull Request hacia main.
# Ampliar más adelante con "push" a main si lo necesitan.
on:
  pull_request:
    branches: [ main ]

jobs:
  validate:
    name: Validar estructura del proyecto
    runs-on: ubuntu-latest

    steps:
      - name: Descargar el código del PR
        uses: actions/checkout@v4

      - name: Verificar que docker-compose.yml es válido
        run: docker compose config

      # --- Placeholders para cuando haya código real ---
      # Descomentar y ajustar según el lenguaje que definan.

      # - name: Lint (Python)
      #   run: |
      #     pip install flake8
      #     flake8 .

      # - name: Lint (Node/JS)
      #   run: |
      #     npm ci
      #     npm run lint

      # - name: Tests
      #   run: |
      #     echo "Agregar comando de tests cuando existan servicios"

      - name: Placeholder - todavía no hay tests
        run: echo "CI funcionando. Agregar lint/tests cuando el equipo defina el stack."